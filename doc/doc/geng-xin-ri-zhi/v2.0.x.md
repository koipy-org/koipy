# v2.0.X

<details>

<summary>2.0.3</summary>

### 2.0.3-dev (2026-07-24)

1. 重写了 miaospeed 客户端实现，使用面向对象编程思想解耦模块
2. 更新 geoip 脚本
3. 修复进度条百分比数值异常溢出的问题
4. 修复 Telegram 话题模式下导致无法发送测试结果的问题

</details>

<details>

<summary>2.0.2</summary>

### 2.0.2-dev (2026-07-20)

1. 小幅优化了启动速度

### 2.0.2-dev (2026-07-16)

1. 修复内置的订阅转换无法解析纯 URI
2. 优化本地化系统
3. 固定 CA 证书，使用 koipy 自身携带的 CA 证书，现在系统根证书不值得信任
4. 支持 mtls 双向证书验证，即 koipy（客户端）可以验证后端的证书，koipy 也可以提供自身证书给后端协商验证（需要 miaospeed 版本 4.7.2）
5. 移除无用代码

### 2.0.2-dev (2026-06-15)

1. 支持 age X25519 armor 加密订阅解密，可在 `subscription.age` 中配置
2. 原生实现了本地内置的订阅转换，只需要将 `subconveter.mode=builtin` 即可启用内置转换
3. 指令参数设置的线程上限从 64 提升到 256
4. 内置的订阅转换支持返回原始订阅中的 dns 配置
5. 不安全的后端配置将在绘图结果中显示
6. 新的配置项：`image.showUnsafeTips` 用于关闭不安全的后端提示（请确保你真的明白你的后端通信处于安全隧道中）

</details>

<details>

<summary>2.0.1</summary>

### 2.0.1 (2026-06-14)

1. 修复了语言包切换时判定成功的逻辑
2. 归一化本地化中的占位符
3. 切换语言包后将显示保存配置的按钮
4. 修正配置模板中本地化语言包的地址例子
5. 本地化语言包加载失败时将静默跳过加载
6. 完善英语语言包中的本地化字段
7. 测速线程设置上限从 `64` 调整为 `256`
8. 现在拥有用户权限且在非邀请测试的场景中将默认启用 `runtime.enableDNSInject` 运行时配置
9. 添加了 DNS 动态注入时的日志提示。当 `runtime.enableDNSInject=true` 时，会明确在后台日志中告知
10. 移除 webapi 的废弃路由
11. invite 测试下，每个 UID 仅会同时存在一个会话。意味着无法通过同时发起两个 `/invite`
12. 现在 `webapi.password` 在 `webapi.enable=true` 时是必填配置
13. sub-store 模板样例添加 `fakeSub=true` 的查询参数
14. 修复私聊中无法使用 `/invite` 指令
15. invite 内联键盘的创建优化
16. 重构 `/invite` 中上下文管理
17. 修复错误的超时计时器。之前有 60 秒的接收订阅计时（实际为 57 秒），现已修正为正确数值
18. 修复 invite 极端情况下的变量可能出现空值但并未正确处理的问题
19. 移除内置的 web 面板静态路由
20. 将 `webapp` 配置项更改为 `webapi`，功能不变
21. 添加更多协议解析（OpenVPN 等）
22. 修正了 mihomo dns 覆写的优先级。现在 `runtime.enableDnsInject` 优先于后端配置里 `DNSServer`
23. 移除了内部函数的 quote 参数
24. 优化了多后端拓扑图的排版问题
25. 更新依赖文件
26. 改进 Web 配置 API 安全策略，强化来源校验并增加鉴权失败限流
27. 优化 Web 配置管理入口，新增 Koiboard 路由及静态资源兼容
28. 指令参数支持多后端指定，做法是写多个 `s=<后端id>`

### 2.0.1-dev (2026-06-06)

1. 修复私聊中无法使用 `/invite` 指令
2. invite 内联键盘的创建优化
3. 重构 `/invite` 中上下文管理
4. 修复错误的超时计时器。之前有 60 秒的接收订阅计时（实际为 57 秒），现已修正为正确数值
5. 修复 invite 极端情况下的变量可能出现空值但并未正确处理的问题

### 2.0.1-dev (2026-05-30)

1. 移除内置的 web 面板静态路由
2. 将 `webapp` 配置项更改为 `webapi`，功能不变
3. 添加更多协议解析（OpenVPN 等）
4. 修正了 mihomo dns 覆写的优先级。现在 `runtime.enableDnsInject` 优先于后端配置里 `DNSServer`
5. 移除了内部函数的 quote 参数
6. 优化了多后端拓扑图的排版问题
7. 更新依赖文件

### 2.0.1-dev (2026-05-07)

1. 改进 Web 配置 API 安全策略，强化来源校验并增加鉴权失败限流
2. 优化 Web 配置管理入口，新增 Koiboard 路由及静态资源兼容
3. 指令参数支持多后端指定，做法是写多个 `s=<后端id>`
4. 指令参数新增 `t` / `d` 短别名，与 `thread` / `duration` 效果相同
5. 升级 bot 依赖 kurigram 到 2.2.22
6. webapi 功能已实现，现在可以通过 HTTP 请求的方式来管理 koipy 的配置文件
7. 针对 webapi 功能写了一个 webapp，你可以在 @koipybot 的简介页面点击 webapp 按钮即可打开使用
8. 新的配置：`webapp` 子类，可用于开启 webapi，配合 web 面板使用，前往配置文件实例查看具体配置
9. webapp 注意事项。建议配合 Nginx 反向代理，否则可能只有回环地址的 api 能访问

</details>

<details>

<summary>2.0.0</summary>

### 2.0.0 (2026-04-09)

🎉 期待已久的正式版发布啦

📜 更新内容

1. 往期 dev 版本的所有内容
2. 修复了多后端联测测速时「每秒速度」的颜色块数值异常问题
3. 新的 runtime 配置：`runtime.enableDNSInject`，用于给后端注入 mihomo 的 dns 配置，让后端能够使用 mihomo 配置里的 dns 进行节点地址解析。这对某些自建 dns 的代理提供商有奇效
4. 修复了 invite 下的并发竞态条件问题
5. 现在后端配置里的 `dnsServer` 可以输入类似 `mihomo://<经过base64编码后的mihomo配置文件>` 来给后端使用 mihomo 的 dns 解析功能（需后端版本至少 4.5.5）

#### Mihomo dns 客户端集成

miaospeed 服务器在接受到来自客户端（主端）的请求时，会将请求里的 `dnsServer` 配置字段进行解析。在本次更新中，添加了一种内部协调的 scheme 用来解析 dns 配置：

```text
mihomo://<Base64编码后的mihomo配置>
```

这个内容等同于 mihomo 配置中的 `dns` 字段，参见：[mihomo DNS 配置文档](https://wiki.metacubex.one/config/dns/)

假设你有如下 dns 配置：

```yaml
dns:
  enable: true
  cache-algorithm: arc
  prefer-h3: false
  use-hosts: true
  use-system-hosts: true
  respect-rules: false
  listen: 0.0.0.0:1053
  ipv6: false
  default-nameserver:
    - 223.5.5.5
  enhanced-mode: fake-ip
  fake-ip-filter-mode: blacklist
  fake-ip-filter:
    - '*.lan'
  nameserver-policy:
    '+.arpa': '10.0.0.1'
    'rule-set:cn':
    - https://doh.pub/dns-query
    - https://dns.alidns.com/dns-query
```

经过 base64 编码加上 scheme 后是这样的：

```text
mihomo://ZG5zOgogIGVuYWJsZT...(已省略)
```

这样的配置可以被 miaospeed 正确识别，使用集成的 mihomo dns 服务进行解析，这样就可以确保节点的 server 能够正确解析到 IP。

### 2.0.0-dev (2026-03-20)

1. 修复多后端翻页按钮
2. 优化多后端拓扑的绘图排版
3. 现在测试脚本返回 `background`（全小写）字段会让绘图背景色生效
4. 修复了深色背景图下协议 Logo 显示异常的问题

### 2.0.0-dev (2026-03-19)

1. 修复 Netflix 检测脚本
2. 修复 Tiktok 检测脚本
3. 修复通过 miaospeed scheme 添加后端时初始化后端配置未成功的问题
4. 移除冗余文件
5. 修复多后端联测时消耗流量统计的数值错误
6. 重构了整个订阅转换功能
7. 移除 substore 及其所有子配置（包括自动部署）
8. 新增了实用脚本位于 `utils/substore_deploy.py`，可以自动部署 sub-store
9. 修复了大量的 invite 测试场景中的内存占用问题
10. 现在订阅转换需要重新书写配置，否则无法使用订阅转换，新的配置示例已更新位于 `config.yaml.example`

### 2.0.0-dev (2026-02-14)

1. 修复旧版许可证导致的多后端选择异常
2. 修复可能的翻页页数错误重定向

### 2.0.0-dev (2026-02-14)

1. 多后端测速任务提供三种调度模式：流水线模式、并发模式、串行模式
2. 多后端的测速任务默认采用新的调度模式——流水线模式
3. 新的配置项 `slaveConfig.speedScheduling`，后端测速任务的调度模式，共有以下可用值：`["concurrent", "pipeline", "sequential"]`，默认 `pipeline`
4. 修正了多后端拓扑图的页脚后端信息显示异常
5. 多后端拓扑图的信息合并逻辑有部分情况未生效，正在修复中，但不影响数据准确性

三种调度模式的含义：

```text
流水线模式：当第一个后端返回第一个节点的测速结果时，下一个后端才开始测试
并发模式：所有后端同时开始测试
串行模式：当前一个后端完成测速任务时，下一个后端才开始测速
```

### 2.0.0-dev (2026-02-12) 🎆 2026 农历新年特别版

本次更新为**重大更新**，为**正在开发**版本，追求稳定请勿更新。

1. 重构了所有选择按钮的实现
2. 重构了所有翻页按钮的实现
3. 重构了所有文本按钮的实现
4. 协议相关 Logo 启用缓存，防止重复在内存渲染。缓存文件在 `./resources/image/` 查看
5. 允许通过配置文件禁用所有测试指令，`/re` 除外
6. 更新本地化语言包
7. 修复了部分情况下进度条的节点计数错误
8. 改进了消息编辑队列，多人公开测试的场景中更为稳定
9. 优化了许可证中的激活码结构
10. 新增指令 `/license` 可用于展示许可证信息
11. 发送 koipy 的激活码即可打印许可证信息
12. 优化许可证的信息展示，`/panel` 页面移除了许可证信息
13. 赞助用户可以任意定义自己的赞助的版本信息，方式为在语言包中覆写此键值：`license_parse_version2`
14. `/id` 现在发送的过长消息默认折叠
15. 支持多后端联测。可以同时将一个任务分发给多个后端，并汇总结果到主端进行绘图
16. 实现多后端联测的按钮选择页面
17. 实现多后端联测的结果绘图功能（包括流媒体、速度、拓扑测试图）
18. 实现多后端联测的进度条功能
19. 实现多后端联测的数据清洗功能
20. 现在选择后端按钮的页面支持选择多个后端（选择单个后端即回退到之前的模式）
21. 脚本选择按钮现在无需点击「完成选择」即可完成勾选时的记忆
22. 实现规则配置中的多后端设置。即 `rule.slaveid` 配置项为一个数组时，就表示启动多后端联测
23. webapp 现在已经初步可用，可前往 @koipybot 的详情页点击「打开应用」，然后输入 bot-token 和 chat_id 即可使用
24. 支持通过 webapp 进行重载配置
25. 实现多后端联测的「中止测试」按钮功能
26. 改进了多后端联测时的错误处理和错误信息展示
27. 优化了多后端联测模式下绘图文本信息的「合并」逻辑
28. 当触发 `/invite` 指令的目标不是用户权限时，仅允许单后端选择
29. 多后端选择页面现在支持显示「延迟」信息

#### 2026 农历新年特别版（2.0.0-dev）说明

1. 此版本允许**所有许可证**类型进行公开测试
2. 此版本的「标准版」许可证最多可以进行 3 个后端的联测（够你三网测试），超过 3 个后端进行联测时会自动取前 3 个后端

#### 关于多后端联测

多后端联测是指，对同一个测试任务，将其分配给多个后端，并同时进行测试，并将结果汇总给主端。

**特性说明**

1. 每个后端的配置可能不一致（比如线程这些），这部分还需要优化
2. 绘图结果的线程显示仅会显示其中一个后端的
3. 绘图结果的页脚的后端名称显示将固定为「集群后端」
4. 拓扑测试的结果可能因为 GEOIP 脚本的逻辑影响，导致结果不一致
5. 多后端联测暂时无法进行「实时渲染」功能
6. 解锁统计在此模式无法生效

</details>
